# Copy to .env.local for local overrides (not committed).
# On Vercel: Project → Settings → Environment Variables → add VITE_API_URL for Production (and Preview if needed).
#
# Full API base URL including /api, no trailing slash:
# VITE_API_URL=https://edutrack-backend-five.vercel.app/api
